tls:
  stores:
    default:
      defaultCertificate:
        certFile: /certs/origin.pem
        keyFile:  /certs/origin-key.pem
  certificates:
    - certFile: /certs/origin.pem
      keyFile:  /certs/origin-key.pem
